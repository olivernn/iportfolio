<h1>Editing Video</h1>

<% form_for([@project, @video], :html => {:multipart => true}) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', [@project, @video] %> |
<%= link_to 'Back', project_path(@project) %>

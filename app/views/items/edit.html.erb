<h1>Editing item</h1>

<% form_for([@project, @item]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', [@project, @item] %> |
<%= link_to 'Back', project_items_path(@project) %>

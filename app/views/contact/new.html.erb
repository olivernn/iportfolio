<h1>Send Message</h1>

<% form_for(@contact_form, :url => {:controller => 'contact', :action => 'create'}) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <p>
    <%= f.submit "Send" %>
  </p>
<% end %>

<%= link_to 'Back', root_path %>